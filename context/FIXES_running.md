# Fixes — running inbox — PitchPlease

*Quick-capture for small fixes that don't need a full SPEC. ☐ reported · ◐ fixing · ✅ shipped.*

---

- ✅ **P0 "app fries" bug — diagnosed AND fixed 2026-06-12** (verified in Chrome: mic-wait state with live tab bar, error message on 10s timeout, End Session exits from any state, mid-wait escape leaves no zombie, triple-fire re-entry yields one clean session, drone/session views truly `display:none` when inactive). Happy path (mic granted) still needs one manual session on Simone's machine. Original diagnosis below: Root cause chain in `session-view.js activate()` (line 85-135): view is shown and tab bar hidden FIRST, then `await mic.start()` (line 117) with no timeout/catch/cancellation, and only THEN is the runner created. Consequences, all verified:
  1. **Bricked session**: mic prompt pending/denied → blank session screen, tab bar hidden, and Pause/Skip/End all silently no-op because `#end()` guards `if (!this.#runner) return` (line 338) and the runner never got created. No escape except killing the app.
  2. **Zombie resurrection** ("all screens at once"): `deactivate()` doesn't cancel the pending `activate()` — a late mic grant resumes it, starting a runner + bus subscriptions UNDER whatever view is now active. A second session then runs two runners concurrently toggling summary/transition/controls against each other.
  3. Compounding fragility: `#drone-view`/`#session-view` ID selectors force `display:flex`, defeating `.view { display:none }` (specificity); three visibility mechanisms coexist (`.active` class, ID overrides, `[hidden]` vestiges); `session:activate` handler in `app.js:120-133` duplicates switchView but omits `sessionView.deactivate()` for an already-running session.
  Fix shape: activation token (stale continuations abort), mic acquisition with timeout + visible error state BEFORE entering session UI, End Session works unconditionally, collapse to one visibility mechanism.
- ✅ **Graph tap-to-pause (Simone, 2026-06-12) — shipped & verified in Chrome.** Quick tap in the canvas center toggles pause/resume (alias for the mic button, same state — not a second pause state); YouTube-style ⏸/▶ flash confirms the toggle. Edge-label press-and-hold-to-drone untouched (verified: label tap doesn't pause). Flash only fires on a real state flip (won't mislead on a denied mic). Happy-path pause→resume loop still wants one manual check on a real mic.
- ✅ **Tuner sliding visualizer (Simone, 2026-06-12) — shipped & verified in Chrome.** Replaced the single-note cents needle with a horizontal scrolling chromatic strip (PitchStrip): fixed center crosshair = your pitch, note ticks scroll past it (piano layout, low left/high right), nearest tick locks green on center in tune. Verified in/sharp/flat via synthetic bus events. Fixed a latent canvas-sizing bug (display:none at init → width 0) with a ResizeObserver. Real-mic feel still wants one manual check.
- ✅ **Graph: preserve trace across pause/resume — shipped in the 2026-07-09 rebuild.** Pause freezes the virtual clock and holds the trace ("PAUSED — TRACE HELD" chip); resume reconnects with no gap. Verified in Chrome with a synthetic tone.
- ✅ **Tuner "flashes around, doesn't steady" (Simone, real-mic feedback 2026-06-13) — fixed & verified.** Root cause: detector emits raw pitch every frame (jitter + octave spikes), and the strip/cents took it unfiltered. Fix: 5-frame median in tuner-view steadies pitch before driving strip/note/cents/Hz; strip color derives from smoothed position. Verified spike rejection + jitter compression via synthetic bus stream. If still too loose in real use → bump window to 7 or add an in-tune deadzone/snap (the "little blocks" idea, held in reserve).
- ✅ Tuner screen empty-void layout — superseded by the 2026-07-09 rebuild (new composition: pixel readout + native token + strip + desc + dithered texture).
- ✅ Drone screen ("stupid and ugly") — resolved by retirement 2026-07-09: the drone is now a graph layer (press-hold any rail label, or the DRONE button locks the scale root). The chord-pad tab is gone.
- ✅ Session no-mic blank state — obsolete: session view deleted in the rebuild.
